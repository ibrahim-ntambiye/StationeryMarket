@model Stationery.Marker

@{
    ViewBag.Title = "DeleteMarkerFromDb";
}

<h2>DeleteMarkerFromDb</h2>

<h3>Are you sure you want to delete this?</h3>
@using (Html.BeginForm("DeleteMarkerFromDb", "Markers", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Marker</h4>

        <h1>Search for MarkerId</h1>
        <input type="text" name="markerId">

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Delete Marker" class="btn btn-default" />
            </div>
        </div>
    </div>
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        @Html.ActionLink("Back to List", "Index")
    </div>
}
</div>
